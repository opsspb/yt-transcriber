# yt-transcriber
A CLI Python tool to get diarized transcripts from YouTube videos using WhisperX.
Works on Mac with Apple Silicon only.

To run, use the following command from Terminal:
python3 yt_diarizer.py
